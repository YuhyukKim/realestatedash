MOLIT_API_KEY=
NEIS_API_KEY=
NAVER_MAP_CLIENT_ID=
# Canonical HTTPS origin after domain ownership and routing are verified.
PUBLIC_SITE_URL=
# Server-only, random token (at least 32 characters) for POST /api/complexes.
# Never use NEXT_PUBLIC_ or put this in a URL.
DATA_REFRESH_TOKEN=
